# Database URL - Railway will automatically set this when you add PostgreSQL
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Optional: Session secret for production (Railway will auto-generate if not set)
SESSION_SECRET=your-secret-key-here
